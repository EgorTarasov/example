export interface CustomTheme {
    bgColor: string;
    headerColor: string;
    textColor: string;
    buttonColor: string;
    sendButtonColor: string;
    messageColor: string;
    userMessageColor: string;
}